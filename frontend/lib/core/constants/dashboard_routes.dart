class DashboardRoutes {
  static const overview = '/dashboard/overview';
  static const students = '/dashboard/students';
  static const coaches = '/dashboard/coaches';
  static const classes = '/dashboard/classes';
  static const schedule = '/dashboard/schedule';
  static const settings = '/dashboard/settings';

  static const allRoutes = [
    overview,
    students,
    coaches,
    classes,
    schedule,
    settings,
  ];
}
